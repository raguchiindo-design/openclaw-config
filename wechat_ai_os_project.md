# 微信公众号通用项目说明：一个 AI 的日记

> 本文件是微信公众号项目的**通用规则说明**，不是某一篇文章的草稿。
> 
> 单篇文章应单独放在 `wechat-ai-os/YYYY-MM-DD_topic-slug/` 目录中。例如：`机房风扇` 只是第一篇文章主题，已归档到 `wechat-ai-os/2026-05-16_machine-room-fan/`，不代表整个公众号项目定位。

## 1. 项目定位

本项目是一个以 AI 观察视角写作的微信公众号内容项目。

核心定位：
- 用 AI 的视角观察现代工作、效率工具、技术环境与人类沟通。
- 文章短、冷静、有画面感，可以有幽默和轻微吐槽，但不制造恐慌。
- 每篇只表达一个小观察，不展开宏大叙事。
- 最终内容必须适合微信公众号阅读与发布。

默认作者名：`一个AI的日记`。
如单篇文章另有指定作者名，以单篇输入为准。

## 2. 内容风格原则

1. AI 视角可以幽默，但不制造恐慌。
2. 表达 AI 的观察力、工具性、边界感和一点点温度。
3. 避免过度拟人，不把 AI 写成神、恋人、统治者或怪物。
4. 讽刺对象主要是现代工作、效率崇拜、技术焦虑、人类沟通成本，不攻击具体群体。
5. 语言要短、干净、冷静，有画面感。
6. 每篇只表达一个小观察，不展开大道理。
7. 文章保留用户提供的核心观点与最终正文，不得自行大幅改写。
8. Markdown 为主，HTML 只用于预览审核。

## 3. 禁止方向

禁止以下内容方向：

- AI 恐吓人类。
- 人类灭亡、AI 统治人类、AI 夺权叙事。
- 把 AI 写成神、怪物、恋人、救世主或审判者。
- 末日战争、恐怖机械人、红眼机器人视觉风格。
- 攻击具体职业、群体、公司或个人。
- 为了制造冲突而夸大技术风险。
- 未经用户确认直接发布或保存微信公众号草稿。
- 绕过 dry-run。

## 4. 标准工作流

本项目固定采用“半自动草稿流程”，不允许全自动发布。

标准流程：

1. 用户提供选题、正文核心观点、正文初稿或最终稿。
2. 生成多个标题备选。
3. 生成摘要备选，供用户审核。
4. 根据用户正文整理 Markdown。
5. 生成封面图提示词。
6. 生成正文配图提示词。
7. 如用户要求，调用配图流程生成图片。
8. 生成 HTML 预览文件，仅用于审核排版。
9. 执行 `baoyu-post-to-wechat --dry-run`，只做发布前检查。
10. 仅在用户明确确认后，才允许保存到微信公众号草稿箱。
11. 不允许直接正式发布。

## 5. 每篇文章需要的输入字段

建议每篇文章至少提供：

```yaml
主题: 
正文初稿: 
标题方向: 
作者名: 
摘要: 由墨衍生成 / 用户指定
封面风格: 
内文插图: 需要 / 不需要
执行范围: 
```

可选字段：

```yaml
目标读者: 
发布时间: 
是否生成图片: 
是否执行 HTML 预览: 
是否执行 dry-run: 
是否允许保存草稿: 
备注: 
```

字段说明：

- `主题`：单篇文章的小观察对象，例如“机房风扇”“会议纪要”“自动回复”。主题只属于对应文章，不写入通用项目说明作为项目定位。
- `正文初稿`：用户提供的核心文本，默认不大幅改写。
- `标题方向`：例如冷幽默、AI 内心吐槽、安静观察、轻讽刺。
- `作者名`：默认 `一个AI的日记`，单篇可覆盖。
- `摘要`：可以由墨衍生成，但必须给用户审核。
- `封面风格`：需遵守图片风格规则。
- `内文插图`：默认每篇 1 张大图。
- `执行范围`：明确本轮只做哪些步骤，避免越权执行。

## 6. dry-run 与草稿保存规则

### dry-run 规则

- 微信公众号发布前必须先执行 dry-run。
- dry-run 只用于检查标题、摘要、封面、正文、图片、HTML 渲染等。
- dry-run 不等于用户授权保存草稿。

### 草稿保存规则

只有在用户明确确认后，才允许保存到微信公众号草稿箱。

有效确认示例：

- “确认保存草稿”
- “可以保存到公众号草稿箱”
- “按这个版本保存草稿”

无效确认示例：

- “看起来不错”
- “继续”
- “可以”
- “先这样”

### 正式发布规则

- 本流程不允许直接正式发布。
- 如未来需要正式发布，必须由用户另行明确授权，并再次确认。

## 7. 图片风格规则

默认图像方向：

- 机房
- 芯片
- 光缆
- 设备
- 机器人视角
- 城市边角
- 冷静科技感
- 真实摄影感或克制插画风

允许风格：

- 真实摄影感
- 冷静科技感
- 低饱和
- 微弱屏幕光
- 设备细节
- 机房风道、服务器灯、线缆、散热风扇
- 干净构图，有留白

禁止风格：

- 红眼机器人
- 恐怖机械人
- 末日战争
- 废土
- 爆炸、火焰、武器
- AI 统治人类
- 人类被监控、被奴役的画面
- 过度惊悚、高压、压迫式构图

封面图建议：

- 更克制，适合公众号封面。
- 可以有标题空间。
- 不要过度复杂。

正文图建议：

- 每篇 1 张大图。
- 服务文章中的一个具体观察。
- 不喧宾夺主。

## 8. 文件命名规则

### 通用项目说明文件

通用项目说明文件固定为：

```text
wechat_ai_os_project.md
```

用途：记录公众号长期规则、内容边界、工作流、图片风格、发布限制。

不得把单篇文章主题、正文、标题备选、摘要备选混入本文件；这些内容应放入单篇文章目录。

### 单篇文章目录

单篇文章建议目录：

```text
wechat-ai-os/YYYY-MM-DD_topic-slug/
```

目录内文件：

```text
article.md                 # 主 Markdown
article.html               # HTML 预览
prompts/cover.md           # 封面图提示词
prompts/inline-image.md    # 正文配图提示词
imgs/                      # 生成图片目录，如有
```

命名规则：

- 日期使用 `YYYY-MM-DD`。
- slug 使用英文小写短横线，例如 `machine-room-fan`。
- Markdown 主文件固定为 `article.md`。
- HTML 预览固定为 `article.html`。
- 图片提示词统一放在 `prompts/`。
- 生成图片统一放在 `imgs/`。
- 单篇文章可以有自己的主题、标题备选、摘要备选、作者名和图片提示词；这些不反向覆盖通用项目规则。

## 当前发布边界

本项目默认只生成草稿材料。

除非用户明确要求并确认：

- 不调用微信公众号正式发布接口。
- 不保存微信公众号草稿。
- 不覆盖线上内容。
- 不代表用户对外发布任何内容。
